version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8000
    container_name: yield-engine-web
    ports:
      - "3000:3000"
